#pragma once
#include <iostream>

class cpu {
	public:
		/*
		 * Registers
		 */
		static uint8_t A;
		static uint8_t B;
		static uint8_t C;
		static uint8_t E;
		static uint8_t H;
		static uint8_t L;
		static uint8_t PC; // Program counter
		static uint8_t SP; // Stack Pointer

}
